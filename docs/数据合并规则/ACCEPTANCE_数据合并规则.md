# 阶段5/6：Automate + Assess（数据合并规则验收记录 - 公共字段）

## 验收用例
- 用例1：未选择公共字段但选择了合并方式 → 按钮应禁用并提示“请先选择公共字段”。
- 用例2：选择了公共字段但该字段不存在于任一侧或类型不兼容 → 面板与统一提示，按钮禁用。
- 用例3：无公共字段候选（交集为空） → 面板展示提示“暂无公共字段可用于合并”，按钮禁用。
- 用例4：选择有效公共字段 + Inner → 修正错误后按钮可用。
- 用例5：选择有效公共字段 + Left/Outer → 修正错误后按钮可用。

## 结果记录（本地预览）
- 预览地址：`http://localhost:3000/`
- 终端：Vite 开发服务器运行中（ID: c1794560-ab32-401f-b429-b10afeb64dd0）。
- 初步结论：公共字段下拉与禁用逻辑正常；后续根据人工点击验证细节补充截图与说明。

## 评估指标
- 代码质量：遵循组件风格与函数注释规范；
- 测试质量：覆盖三种合并方式与公共字段选择相关错误；
- 文档质量：与对齐/设计一致；
- 集成：与既有规则无冲突；
- 技术债务：原型阶段控制在 UI 层，后端留空。