# TODO — 任务管理简化改造（待办与缺口）

更新时间：2025-10-31 00:20（UTC+8）

## 待办项
- 检查其他模块是否存在 JSON 模式相关引用（例如 TaskTypeManagement.tsx 中的 JSON 导入函数仅与任务类型管理相关，暂不影响本模块，如需统一策略后续评估）。
- 在 CHANGELOG.md 中记录“移除 JSON 配置模式、统一对象模式 config”的变更条目。
- 根据后续演示需要，是否为页面配置表单提供“预设模板”预填能力（替代 JSON 导入）。

## 缺少的配置或外部依赖
- 无（前端原型使用模拟数据）。

## 操作指引
- 若需要兼容历史数据（字符串 JSON 的 task.config），请在加载任务时解析为对象后写回（当前原型已在编辑态解析，不再提供 JSON 文本编辑入口）。

